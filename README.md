# CS 12 23.2 Lab 10

## Prerequisites

Navigate to your local repo root, then run the following _(replace `pip` with `pip3` or `python -m pip` if needed)_:
```bash
pip install -r requirements.txt
```
